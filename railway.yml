version: 3
services:
  - name: carselling
    type: java
    buildCommand: ./mvnw clean install
    runCommand: java -jar target/demo-0.0.1-SNAPSHOT.jar

logging:
  level:
    org.springframework.security: DEBUG
